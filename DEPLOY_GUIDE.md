# 🚀 로컬 빌드 및 배포 가이드 (Vision Board 2026)

이 문서는 프로젝트 개발 과정에서 로컬 환경에 최신 코드를 배포하고, 상태를 점검하는 과정을 정리한 가이드입니다. 

---

## 1. 로컬 빌드 및 배포 방법

본 프로젝트는 순수 HTML/JS/CSS로 구성되어 별도의 복잡한 컴파일 과정은 없으나, **'서버 환경(http://)'**에서 구동하는 것이 중요합니다.

### A. 일반적인 개발 방법 (추천)
*   **방법**: VS Code의 **Live Server** 확장 프로그램 사용
*   **실행**: `index.html` 우클릭 -> `Open with Live Server`
*   **포트**: 기본 `5500` (Live Server 기본 설정)
*   **💡 포트 구분**: 포트 번호(5500, 8080 등)는 서버의 '입구' 번호입니다. Live Server는 5500을, 우리가 직접 띄운 서버는 8080을 사용하므로 주소가 서로 다릅니다.
*   **장점**: 코드 수정 시 브라우저가 자동으로 새로고침되어 실시간 확인 가능
*   **💡 프롬프팅 예시**: "VS Code 환경이야. Live Server를 사용해서 5500 포트로 개발 서버 띄워주고 자동 새로고침 설정해줘."

### B. 도구 부재 시 긴급 서버 배포 (PowerShell)
Node.js(`npm`)나 가상 환경이 설치되지 않은 환경에서 윈도우 기본 도구로 서버를 띄우는 방법입니다.
1.  프로젝트 폴더에서 PowerShell 실행
2.  아래 스크립트 실행 (8080 포트 점유):
    ```powershell
    $listener = New-Object System.Net.HttpListener;
    $listener.Prefixes.Add("http://localhost:8080/");
    $listener.Start();
    while ($listener.IsListening) { 
        # (생략된 간이 HTTP 응답 로직 - DEPLOY_GUIDE 하단 참조)
    }
    ```
3.  **💡 프롬프팅 예시**: "Nodejs나 설치된 도구가 없어. 윈도우 기본 PowerShell을 사용해서 8080 포트에 로컬 서버 배포하고 접속 주소 알려줘."

### C. React 프로젝트의 경우 (프레임워크 환경)
프로젝트가 순수 HTML이 아닌 **React(Vite, CRA 등)**로 구성된 경우, 브라우저가 읽을 수 있는 형태로 변환하는 **'빌드(Build)'** 과정이 필수입니다.

1.  **개발 서버 구동**: 수시로 반영 사항을 확인할 때
    - `npm run dev` (Vite) 또는 `npm start` (CRA)
    - 특징: 소스 코드를 실시간으로 감시하며 메모리 상에서 즉시 반영
2.  **프로덕션 빌드**: 실제 배포용 파일을 만들 때
    - `npm run build`
    - 결과: `dist/` 또는 `build/` 폴더가 생성되며, 최적화된 HTML/JS/CSS 파일들이 담김
3.  **빌드 결과물 확인**:
    - 생성된 `dist/` 폴더 안으로 이동하여 위에 설명한 `npx serve` 혹은 `PowerShell 서버`를 실행해야 함
    - 주의: React의 소스코드(`.jsx`, `.tsx`)는 브라우저가 직접 읽지 못하므로 반드시 빌드 결과물을 서버에 올려야 함
4.  **💡 프롬프팅 예시**: "React 프로젝트야. `npm run build`로 최적화된 파일을 생성하고, `dist` 폴더를 기준으로 로컬 서버 배포해줘."

---

## 2. 현황 파악 및 배포 체크리스트

최신 코드가 제대로 배포되었는지 확인하기 위해 다음 항목을 점검하세요.

- [ ] **Git 상태 확인**: `git status`를 통해 로컬 작업 내역이 모두 커밋되었는지 확인합니다.
    - `working tree clean`이 나와야 최신 코드가 안전하게 저장된 상태입니다.
- [ ] **레이어(z-index) 정상 동작**: 스티커를 'Back'으로 보냈을 때 다시 선택(클릭)이 가능한가?
    - (해결책: z-index 최솟값을 10 이상으로 유지하여 바닥 뒤로 숨지 않게 함)
- [ ] **이미지 CORS 정책 점검**: 외부 사이트(Pinterest 등)의 이미지를 추가했을 때 앱이 멈추지 않는가?
    - (해결책: `getDominantColor` 함수에 try-catch 예외 처리가 반영되어 있어야 함)
- [ ] **관리 페이지 연동**: 상단 [관리] 버튼 클릭 시 테이블 목록이 정상적으로 출력되는가?

---

## 3. 실패 사례 및 예외 케이스 정리

### ❌ 사례 1: 스티커 선택 불가 (Unreachable Stickers)
*   **원인**: `zIndex`가 0 이하로 내려가서 보드 배경판보다 뒤로 숨음.
*   **대응**: 최소 `zIndex`를 10으로 고정하고, 더 뒤로 보낼 때는 다른 스티커들의 점수를 높이는 '지능형 밀기' 로직 사용.

### ❌ 사례 2: 외부 이미지 분석 오류 (CORS Issue)
*   **원인**: 외부 보안 서버에 있는 이미지는 브라우저가 색상 데이터(Canvas) 추출을 차단함.
*   **대응**: 이미지 로드 로직에 예외 처리를 추가하여, 분석 실패 시 기본값(연보라색 등)으로 자동 대체.

### ❌ 사례 3: 수정 후 레이어 초기화 문제
*   **원인**: 프로젝트를 수정하거나 클릭할 때마다 `max(zIndex) + 1` 로직이 실행되어 레이어가 맨 앞으로 튀어나옴.
*   **대응**: 자동 `bring-to-front` 로직을 제거하고 개발자가 의도적으로 [Front] 버튼을 눌렀을 때만 변경되도록 수정.

### ❌ 사례 4: Git 커밋 멈춤 현상 (COMMIT_EDITMSG)
*   **원인**: `git commit` 명령 시 메시지(`-m`)를 누락하면, 깃은 메시지 입력을 위해 에디터 창(`COMMIT_EDITMSG`)을 엽니다. 이 창이 닫히기 전까지는 커밋이 완료되지 않고 멈춰있는 것처럼 보입니다.
*   **대응**: 열려 있는 `.git/COMMIT_EDITMSG` 파일 첫 줄에 메시지(예: `레이아웃 오류 해결` 또는 `initial commit`)를 입력하고 **저장 후 창을 닫으세요.** 그럼 즉시 커밋이 완료됩니다.

---

---

## 🛠️ 부록: PowerShell 서버 전체 스크립트 (참고용)
> 가이드를 따라 직접 서버를 실행해야 할 때 복사해서 사용하세요.
```powershell
$listener = New-Object System.Net.HttpListener;
$listener.Prefixes.Add("http://localhost:8080/");
$listener.Start();
Write-Host "Server started at http://localhost:8080/";
while ($listener.IsListening) {
    $context = $listener.GetContext();
    $request = $context.Request;
    $response = $context.Response;
    $path = $request.Url.LocalPath;
    if ($path -eq "/") { $path = "/index.html" };
    $fullPath = Join-Path (Get-Location) $path;
    if (Test-Path $fullPath -PathType Leaf) {
        $content = [System.IO.File]::ReadAllBytes($fullPath);
        $response.OutputStream.Write($content, 0, $content.Length);
    } else { $response.StatusCode = 404; };
    $response.Close();
}
```
